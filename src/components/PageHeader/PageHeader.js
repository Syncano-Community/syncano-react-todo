import React from 'react'

const PageHeader = () => (
  <header className="page-header">
    <h1>To-Do list application</h1>
    <p>Built on Syncano with React & Redux</p>
  </header>
);

export default PageHeader;